import React, { Component } from "react";

class Chat extends Component {
  render() {
    return (
      <div>
        <input type="text"></input>
        <button>send</button>
      </div>
    );
  }
}

export default Chat;
